apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: demo-environment-check
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  check:
    steps:
      - name: Verify hosted workflow execution
        uses: docker://alpine:3.20
        shell: sh
        run: |
          echo "CloudBees hosted workflow execution is available."
          echo "No application tests or deployments are performed by this diagnostic."
